export const metadata = {
    title: "Monsur - Contacts",
    description: "You can find all my social handle links here.",
};

export default function ContactsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
