export const metadata = {
    title: "Monsur - Projects",
    description: "You can find all my projects here.",
};

export default function ProjectsLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
